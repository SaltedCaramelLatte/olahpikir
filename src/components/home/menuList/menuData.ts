// src/components/home/menuList/menuData.ts
export interface MenuItemType {
    id?: string; // Tambahkan properti id, dan gunakan tanda tanya (?) jika bisa opsional
    title: string;
    img: string;
    price: string;
    description: string;
    status: string;
}
